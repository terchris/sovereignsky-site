{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://totalforsvarsaret.no/schemas/use-area.schema.json",
  "title": "Use Area",
  "description": "Software category/use area definition for SovereignSky",
  "type": "object",
  "required": ["id", "name"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$",
      "description": "Unique identifier for the use area (lowercase, alphanumeric with hyphens/underscores)"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 50,
      "description": "Display name for the use area"
    },
    "icon": {
      "type": "string",
      "description": "Icon identifier (e.g., Lucide icon name)"
    },
    "description": {
      "type": "string",
      "maxLength": 200,
      "description": "Brief description of what this use area covers"
    }
  },
  "additionalProperties": false
}
